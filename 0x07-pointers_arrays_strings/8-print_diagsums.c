#include "main.h"
#include <stdio.h>
/**
 * print_diagsums - prints sum of diagonals os square
 * @a: Input
 * @size: Input
 * Return: none
 */
void print_diagsums(int *a, int size)
{
	printf("%d", a[0]);
	printf("%d", size);
}
